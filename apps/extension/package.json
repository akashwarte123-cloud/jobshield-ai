{
  "name": "@jobshield/extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build --clean && tsc --build && node build.js"
  },
  "dependencies": {
    "@jobshield/shared": "file:../../packages/shared"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "sharp": "^0.35.3",
    "typescript": "^5.3.3"
  }
}
